query Country($countryId: ID!) {
    country(code: $countryId) {
        code
        name
        native
        phone
        capital
        currency
        emoji
        continent {
            code
            name
        }
        languages {
            code
            name
            native
            rtl
        }
        states {
            code
            name
        }
    }
}